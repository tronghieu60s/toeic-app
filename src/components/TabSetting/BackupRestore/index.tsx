import React, { memo } from 'react';
import { ScrollView, Text } from '../../Themed';

export default memo(function TabSettingBackupRestore() {
  return (
    <ScrollView>
      <Text>test</Text>
    </ScrollView>
  );
});
